<<<<<<< Updated upstream
# Roadmap: K8s ML Predictive Autoscaling

Детальный план развития проекта по фазам с оценкой времени, зависимостями и критериями завершения.

---

## Общая временная оценка

| Фаза | Описание | Оценка времени | Статус |
|------|----------|----------------|--------|
| Phase 0 | Инфраструктура и каркас | 1-2 недели | 🟡 In Progress |
| Phase 1 | Сбор и препроцессинг данных | 2-3 недели | ⚪ Not Started |
| Phase 2 | Базовые ML-модели | 3-4 недели | ⚪ Not Started |
| Phase 3 | Гибридная модель и ONNX | 2-3 недели | ⚪ Not Started |
| Phase 4 | Сервис инференса | 1-2 недели | ⚪ Not Started |
| Phase 5 | Интеграция с K8s | 2-3 недели | ⚪ Not Started |
| Phase 6 | Мониторинг и визуализация | 1-2 недели | ⚪ Not Started |
| Phase 7 | Эксперименты и публикация | 3-4 недели | ⚪ Not Started |
| Phase 8 | RL-подход (опционально) | 4-6 недель | ⚪ Not Started |

**Итого**: 15-25 недель (3.5-6 месяцев) для основных фаз (0-7)

---

## Phase 0: Инфраструктура и каркас репозитория

**Цель**: Создать базовую инфраструктуру для разработки и тестирования.

**Время**: 1-2 недели

### Задачи

#### 0.1. Настройка репозитория
- [ ] Создать `requirements.txt` или `pyproject.toml`
- [ ] Настроить pre-commit hooks (black, flake8, mypy)

**Файлы**:
```
.gitignore
LICENSE
requirements.txt / pyproject.toml
.pre-commit-config.yaml
```

#### 0.2. Docker-окружение
- [ ] `Dockerfile` для демо-микросервиса
- [ ] `docker-compose.yml` с сервисами:
  - Prometheus
  - Grafana
  - 2-3 демо-микросервиса (простой FastAPI app)
- [ ] Скрипты инициализации Grafana (datasource, базовый dashboard)

**Файлы**:
```
docker/
  Dockerfile.demo-service
  docker-compose.yml
  prometheus/
    prometheus.yml
  grafana/
    provisioning/
      datasources/
      dashboards/
```

#### 0.3. Kubernetes манифесты (локально)
- [ ] Настроить kind / minikube конфигурацию
- [ ] Манифесты для демо-сервисов (Deployment, Service)
- [ ] Prometheus Operator или Helm chart
- [ ] Базовый HPA манифест (для сравнения)

**Файлы**:
```
k8s/
  kind-config.yaml
  manifests/
    namespace.yaml
    demo-service-deployment.yaml
    demo-service-service.yaml
    prometheus/
    hpa/
      baseline-hpa.yaml
```

#### 0.4. Документация
- [ ] README.md 
- [ ] ROADMAP.md 
- [ ] CONTRIBUTING.md (базовая версия)
- [ ] docs/setup-guide.md (как поднять локальное окружение)

### Критерии завершения Phase 0
- ✅ Локально запускается docker-compose с Prometheus, Grafana, demo-services
- ✅ Демо-сервис экспонирует `/metrics` и собирается Prometheus
- ✅ В Grafana можно увидеть метрики CPU/память демо-сервиса
- ✅ Kind/minikube кластер поднимается и деплоятся демо-сервисы
- ✅ Есть базовая документация по запуску

---

## Phase 1: Сбор и препроцессинг данных

**Цель**: Собрать исторические данные из Prometheus и подготовить их для обучения моделей.

**Время**: 2-3 недели

**Зависимости**: Phase 0

### Задачи

#### 1.1. Сбор данных из Prometheus
- [ ] Реализовать `src/collector/prometheus_client.py`:
  - Подключение к Prometheus API
  - Запросы PromQL для основных метрик:
    - `container_cpu_usage_seconds_total`
    - `container_memory_working_set_bytes`
    - Request rate (если есть ingress/service metrics)
    - Latency percentiles (p50, p95, p99)
- [ ] Скрипт `src/collector/collect_historical.py`:
  - Загрузка данных за последние N дней
  - Сохранение в `data/raw/` в формате CSV/Parquet
- [ ] Конфигурация через YAML:
  - Список метрик
  - Временные диапазоны
  - Шаг агрегации (по умолчанию 30s)

**Файлы**:
```
src/
  collector/
    __init__.py
    prometheus_client.py
    collect_historical.py
    config.yaml
data/
  raw/
    cpu_metrics_YYYYMMDD.csv
    memory_metrics_YYYYMMDD.csv
    request_rate_YYYYMMDD.csv
```

#### 1.2. Генерация синтетической нагрузки (опционально)
- [ ] Скрипт для генерации паттернов нагрузки:
  - Дневная сезонность (пики в рабочие часы)
  - Недельная сезонность (спад в выходные)
  - Случайные всплески (имитация traffic spikes)
- [ ] Инструмент нагрузочного тестирования (k6 / Locust):
  - Конфигурационные сценарии
  - Варьирование RPS по времени

**Файлы**:
```
tools/
  load_generator/
    k6_script.js
    locust_tasks.py
    synthetic_patterns.py
```

#### 1.3. EDA (Exploratory Data Analysis)
- [ ] Jupyter notebook `notebooks/research-data.ipynb`:
  - Загрузка и визуализация временных рядов
  - Статистика (среднее, медиана, квантили, стандартное отклонение)
  - Выявление трендов и сезонности (STL decomposition)
  - Обнаружение аномалий (IQR, Isolation Forest)
  - Корреляционный анализ между метриками
  - Автокорреляция (ACF, PACF)

**Результаты**:
- Понимание характера нагрузки
- Выбор подходящих метрик для прогнозирования
- Определение горизонта прогноза (5, 15, 30 минут)

#### 1.4. Препроцессинг данных
- [ ] Реализовать `src/preprocessor/pipeline.py`:
  - **Ресемплинг**: приведение к фиксированному шагу (например, 1 минута)
  - **Обработка пропусков**: интерполяция или forward-fill
  - **Нормализация**: MinMaxScaler или StandardScaler
  - **Фильтрация аномалий**: удаление выбросов (Z-score, IQR)
  - **Feature engineering**:
    - Временные признаки (hour, day_of_week, is_weekend)
    - Лаговые признаки (t-1, t-5, t-15)
    - Скользящие средние (MA, EMA)
    - Разностные признаки (diff)
- [ ] Sliding window генерация:
  - Создание последовательностей (seq_len, features) для LSTM
  - Разделение на train/val/test (70/15/15 или 60/20/20)
- [ ] Сохранение обработанных данных:
  - `data/processed/train.csv`
  - `data/processed/val.csv`
  - `data/processed/test.csv`
  - Scaler объекты (для обратного преобразования)

**Файлы**:
```
src/
  preprocessor/
    __init__.py
    pipeline.py
    feature_engineering.py
    anomaly_detection.py
    config.yaml
data/
  processed/
    train.csv
    val.csv
    test.csv
    scaler.pkl
```

### Критерии завершения Phase 1
- ✅ Собраны исторические данные из Prometheus за N дней/недель
- ✅ Проведён EDA, визуализированы паттерны нагрузки
- ✅ Реализован воспроизводимый pipeline препроцессинга
- ✅ Данные разделены на train/val/test и сохранены
- ✅ Документация по формату данных и признакам

---

## Phase 2: Базовые ML-модели (Prophet, LSTM/GRU)

**Цель**: Обучить и сравнить базовые модели прогнозирования временных рядов.

**Время**: 3-4 недели

**Зависимости**: Phase 1

### Задачи

#### 2.1. Prophet модель
- [ ] Реализовать `models/prophet/train.py`:
  - Загрузка train/val данных
  - Обучение Prophet модели:
    - Настройка компонент (trend, seasonality)
    - Добавление custom seasonality (hourly, weekly)
    - Включение holidays (опционально)
  - Hyperparameter tuning (changepoint_prior_scale, seasonality_prior_scale)
  - Сохранение модели (`prophet_model.pkl`)
- [ ] Реализовать `models/prophet/evaluate.py`:
  - Прогноз на test данных
  - Метрики: RMSE, MAE, MAPE, R²
  - Визуализация: real vs predicted, компоненты (trend, seasonality)
- [ ] Jupyter notebook `notebooks/research-prophet.ipynb`:
  - Эксперименты с параметрами
  - Анализ результатов

**Файлы**:
```
models/
  prophet/
    __init__.py
    train.py
    evaluate.py
    config.yaml
    prophet_model.pkl
    metrics.json
```

#### 2.2. LSTM/GRU модель
- [ ] Реализовать `models/lstm/model.py`:
  - Архитектура LSTM/GRU:
    - Варианты: single-layer, stacked, bidirectional
    - Dropout для регуляризации
    - Dense output layer
  - DataLoader для последовательностей
- [ ] Реализовать `models/lstm/train.py`:
  - Обучение на PyTorch/TensorFlow:
    - Loss: MSE или MAE
    - Optimizer: Adam
    - Learning rate scheduler (ReduceLROnPlateau)
    - Early stopping (на val loss)
  - Сохранение чекпоинтов
- [ ] Реализовать `models/lstm/evaluate.py`:
  - Прогноз на test данных
  - Метрики: RMSE, MAE, MAPE, R²
  - Визуализация
- [ ] Jupyter notebook `notebooks/research-lstm.ipynb`:
  - Эксперименты с архитектурой (hidden_size, num_layers, dropout)
  - Hyperparameter search (Optuna / Ray Tune)

**Файлы**:
```
models/
  lstm/
    __init__.py
    model.py
    train.py
    evaluate.py
    config.yaml
    checkpoints/
      best_model.pth
    metrics.json
```

#### 2.3. Сравнение моделей
- [ ] Notebook `notebooks/comparison.ipynb`:
  - Загрузка всех моделей
  - Прогноз на общем test set
  - Сравнение метрик (таблица, графики)
  - Анализ ошибок по времени суток, дням недели
  - Вычислительная стоимость (training time, inference time)
- [ ] Документ `docs/model-comparison-results.md`:
  - Таблицы результатов
  - Выводы и рекомендации

**Результаты**:
- Определение лучшей baseline модели
- Понимание сильных/слабых сторон каждого подхода

### Критерии завершения Phase 2
- ✅ Обучены и сохранены Prophet и LSTM/GRU модели
- ✅ Проведена оценка на test данных
- ✅ Сравнение моделей задокументировано
- ✅ Выбрана модель-кандидат для дальнейшего улучшения

---

## Phase 3: Гибридная модель Prophet+LSTM и экспорт в ONNX

**Цель**: Реализовать гибридный подход и оптимизировать модели для продакшена.

**Время**: 2-3 недели

**Зависимости**: Phase 2

### Задачи

#### 3.1. Гибридная модель Prophet+LSTM
- [ ] Реализовать `models/hybrid/prophet_lstm.py`:
  - **Этап 1**: Prophet моделирует trend + seasonality
  - **Этап 2**: LSTM прогнозирует остатки (residuals)
  - Комбинированный прогноз: `y_pred = prophet_pred + lstm_residual_pred`
- [ ] Обучение:
  - Сначала обучить Prophet
  - Вычислить residuals: `residuals = y_true - prophet_pred`
  - Обучить LSTM на residuals
- [ ] Оценка:
  - Метрики на test данных
  - Сравнение с базовыми моделями
  - Ожидаемое улучшение: 6-15% по RMSE

**Файлы**:
```
models/
  hybrid/
    __init__.py
    prophet_lstm.py
    train.py
    evaluate.py
    config.yaml
    prophet_component.pkl
    lstm_component.pth
    metrics.json
```

#### 3.2. Экспорт моделей в ONNX
- [ ] Реализовать `models/onnx/export.py`:
  - Экспорт Prophet:
    - Сериализация Prophet предсказаний в lookup table
    - Или использование NeuralProphet (поддерживает ONNX)
  - Экспорт LSTM:
    - `torch.onnx.export()` для PyTorch
    - `tf2onnx` для TensorFlow
  - Экспорт гибридной модели (двухэтапный pipeline)
- [ ] Валидация ONNX моделей:
  - Проверка выходов (ONNX vs оригинальная модель)
  - Бенчмарки inference speed
- [ ] Сохранение в `models/onnx/`:
  - `prophet.onnx`
  - `lstm.onnx`
  - `hybrid.onnx`

**Файлы**:
```
models/
  onnx/
    __init__.py
    export.py
    validate.py
    prophet.onnx
    lstm.onnx
    hybrid.onnx
    benchmark.json
```

#### 3.3. Оптимизация моделей
- [ ] Квантизация (int8) для ускорения inference
- [ ] Pruning (удаление незначимых весов)
- [ ] Оптимизация графа ONNX
- [ ] Сравнение размеров моделей и скорости

### Критерии завершения Phase 3
- ✅ Реализована и обучена гибридная модель Prophet+LSTM
- ✅ Гибридная модель показывает улучшение на 6-15% по RMSE
- ✅ Модели экспортированы в ONNX
- ✅ ONNX модели валидированы и оптимизированы
- ✅ Бенчмарки inference latency задокументированы

---

## Phase 4: Сервис инференса (FastAPI + ONNX Runtime)

**Цель**: Создать production-ready REST API для онлайн-прогнозирования.

**Время**: 1-2 недели

**Зависимости**: Phase 3

### Задачи

#### 4.1. FastAPI приложение
- [ ] Реализовать `src/predictor/app.py`:
  - Эндпоинты:
    - `POST /predict`: прогноз нагрузки на горизонте T минут
    - `GET /health`: health check
    - `GET /models`: список доступных моделей
    - `POST /models/reload`: перезагрузка моделей
  - Загрузка ONNX моделей при старте
  - Обработка запросов с валидацией (Pydantic)
- [ ] Request/Response схемы:
  ```python
  class PredictRequest(BaseModel):
      service_name: str
      horizon_minutes: int  # 5, 15, 30
      model_type: str  # "prophet", "lstm", "hybrid"
  
  class PredictResponse(BaseModel):
      predictions: List[float]
      timestamps: List[datetime]
      confidence_intervals: Optional[Dict]
      model_used: str
  ```

**Файлы**:
```
src/
  predictor/
    __init__.py
    app.py
    models.py
    schemas.py
    config.yaml
```

#### 4.2. ONNX Runtime интеграция
- [ ] Реализовать `src/predictor/inference.py`:
  - Загрузка ONNX моделей (`onnxruntime.InferenceSession`)
  - Предобработка входных данных (нормализация, reshape)
  - Запуск инференса
  - Постобработка (денормализация, confidence intervals)
- [ ] Кэширование прогнозов (Redis, опционально):
  - TTL на прогнозы (например, 1-2 минуты)
  - Уменьшение нагрузки на ML-модель

#### 4.3. Интеграция с Prometheus
- [ ] Реализовать `src/predictor/metrics_fetcher.py`:
  - Получение последних N точек из Prometheus
  - Формирование входной последовательности для модели
  - Обработка пропусков и аномалий

#### 4.4. Тестирование и мониторинг
- [ ] Unit-тесты:
  - Тесты эндпоинтов (pytest + FastAPI TestClient)
  - Мок ONNX инференса
- [ ] Integration тесты:
  - Реальные ONNX модели
  - Проверка корректности прогнозов
- [ ] Логирование:
  - Structured logging (JSON)
  - Request ID для трейсинга
- [ ] Метрики сервиса:
  - Prometheus metrics: request_count, latency, errors
  - Model inference time

#### 4.5. Dockerfile и деплой
- [ ] `Dockerfile` для predictor сервиса:
  - Base image: python:3.11-slim
  - Установка onnxruntime-gpu (если GPU)
  - Копирование ONNX моделей
- [ ] Kubernetes манифесты:
  - Deployment, Service
  - ConfigMap для конфигурации
  - Resource requests/limits

**Файлы**:
```
src/
  predictor/
    inference.py
    metrics_fetcher.py
    cache.py
tests/
  predictor/
    test_app.py
    test_inference.py
docker/
  Dockerfile.predictor
k8s/
  manifests/
    predictor-deployment.yaml
    predictor-service.yaml
```

### Критерии завершения Phase 4
- ✅ FastAPI сервис запускается и отвечает на запросы
- ✅ ONNX Runtime корректно загружает и выполняет модели
- ✅ Интеграция с Prometheus для получения метрик
- ✅ Unit и integration тесты проходят
- ✅ Сервис задеплоен в Kubernetes кластер
- ✅ Латентность инференса < 100ms (для простых моделей)

---

## Phase 5: Resource Planner и интеграция с Kubernetes

**Цель**: Преобразовать ML-прогнозы в решения по масштабированию и интегрировать с Kubernetes HPA/KEDA.

**Время**: 2-3 недели

**Зависимости**: Phase 4

### Задачи

#### 5.1. Resource Planner сервис
- [ ] Реализовать `src/planner/app.py`:
  - Эндпоинты:
    - `POST /plan`: получить прогноз, вернуть план масштабирования
    - `GET /history`: история решений
- [ ] Логика планирования `src/planner/resource_calculator.py`:
  - Вход: прогноз нагрузки (CPU, RPS)
  - Выход: количество реплик, CPU/memory limits
  - Алгоритм:
    1. Определить целевое использование ресурсов (например, 70% CPU)
    2. Рассчитать требуемое количество реплик: `replicas = ceil(predicted_load / (target_utilization * capacity_per_pod))`
    3. Добавить safety margin (10-15%)
    4. Учесть min/max ограничения
  - Сглаживание решений (избежание частых изменений):
    - Hysteresis: изменять только при превышении порога
    - Rate limiting: максимум N изменений за период
- [ ] Политики масштабирования:
  - **Aggressive**: быстрое scale-up, медленное scale-down
  - **Conservative**: медленное scale-up, быстрое scale-down (экономия ресурсов)
  - **Balanced**: средние скорости

**Файлы**:
```
src/
  planner/
    __init__.py
    app.py
    resource_calculator.py
    policies.py
    schemas.py
    config.yaml
```

#### 5.2. Интеграция с Kubernetes HPA (Custom Metrics)
- [ ] Реализовать Custom Metrics API Adapter:
  - Экспонировать ML-прогнозы как custom metrics
  - Или использовать Prometheus Adapter для маппинга
- [ ] Настроить HPA:
  ```yaml
  apiVersion: autoscaling/v2
  kind: HorizontalPodAutoscaler
  metadata:
    name: ml-autoscaler
  spec:
    scaleTargetRef:
      apiVersion: apps/v1
      kind: Deployment
      name: demo-service
    minReplicas: 2
    maxReplicas: 20
    metrics:
    - type: External
      external:
        metric:
          name: predicted_cpu_usage
          selector:
            matchLabels:
              service: demo-service
        target:
          type: AverageValue
          averageValue: "70"
  ```
- [ ] Тестирование:
  - Проверка, что HPA реагирует на ML-метрики
  - Сравнение с baseline HPA (по CPU)

**Файлы**:
```
k8s/
  hpa/
    ml-hpa.yaml
    custom-metrics-config.yaml
```

#### 5.3. Интеграция с KEDA (альтернатива HPA)
- [ ] Настроить KEDA ScaledObject:
  ```yaml
  apiVersion: keda.sh/v1alpha1
  kind: ScaledObject
  metadata:
    name: ml-scaledobject
  spec:
    scaleTargetRef:
      name: demo-service
    minReplicaCount: 2
    maxReplicaCount: 20
    triggers:
    - type: external
      metadata:
        scalerAddress: predictor-service:8080
        query: predicted_load
  ```
- [ ] Реализовать KEDA External Scaler:
  - gRPC сервис для KEDA
  - Методы: `IsActive()`, `GetMetricSpec()`, `GetMetrics()`
  - Возврат predicted load

**Файлы**:
```
src/
  planner/
    keda_scaler.py
k8s/
  keda/
    scaledobject.yaml
    keda-config.yaml
```

#### 5.4. Executor (применение изменений в K8s)
- [ ] Реализовать `src/executor/k8s_client.py`:
  - Использование Python Kubernetes client
  - Методы:
    - `scale_deployment(name, replicas)`
    - `update_resource_limits(name, cpu, memory)`
  - Валидация изменений перед применением
  - Rollback при ошибках
- [ ] Реализовать `src/executor/controller.py`:
  - Периодический опрос planner сервиса
  - Применение рекомендаций через K8s API
  - Логирование всех действий
- [ ] Fallback механизм:
  - При деградации ML-модели (высокая ошибка, timeout)
  - Автоматический откат на baseline HPA
  - Алерты в Prometheus

**Файлы**:
```
src/
  executor/
    __init__.py
    k8s_client.py
    controller.py
    fallback.py
    config.yaml
```

#### 5.5. Безопасность и RBAC
- [ ] Настроить ServiceAccount, Role, RoleBinding:
  - Минимальные права для executor (update Deployments/HPA)
  - NetworkPolicy для изоляции сервисов
- [ ] Аутентификация между сервисами (mTLS или API keys)

**Файлы**:
```
k8s/
  rbac/
    serviceaccount.yaml
    role.yaml
    rolebinding.yaml
```

### Критерии завершения Phase 5
- ✅ Resource Planner корректно преобразует прогнозы в планы масштабирования
- ✅ ML-прогнозы интегрированы с HPA или KEDA
- ✅ Executor применяет изменения в Kubernetes кластере
- ✅ Fallback механизм работает при деградации ML-модели
- ✅ RBAC и безопасность настроены
- ✅ Система масштабирует демо-сервис на основе прогнозов

---

## Phase 6: Дашборды и мониторинг

**Цель**: Визуализировать работу системы, метрики качества и экономию ресурсов.

**Время**: 1-2 недели

**Зависимости**: Phase 5

### Задачи

#### 6.1. Grafana дашборд
- [ ] Создать `dashboard/grafana.json`:
  - **Панель 1**: Реальная нагрузка vs. Прогноз
    - Временной ряд: actual load, predicted load, confidence interval
    - Цветовая индикация точности
  - **Панель 2**: Количество реплик
    - Actual replicas (из K8s)
    - Recommended replicas (из planner)
    - Baseline HPA replicas (для сравнения)
  - **Панель 3**: Метрики качества прогноза
    - RMSE, MAE, MAPE (sliding window)
    - Prediction accuracy over time
  - **Панель 4**: Экономия ресурсов
    - CPU/Memory usage: ML-autoscaler vs baseline HPA
    - Cost savings (в %)
  - **Панель 5**: SLO метрики
    - Latency (p50, p95, p99)
    - Error rate
    - SLO violations
  - **Панель 6**: Сервисы ML-системы
    - Predictor latency, errors
    - Planner decisions rate
    - Executor actions
- [ ] Настроить алерты:
  - Высокая ошибка прогноза (RMSE > порог)
  - Нарушение SLO (latency p95 > порог)
  - Недоступность predictor/planner сервисов

**Файлы**:
```
dashboard/
  grafana.json
  alerts.yaml
```

#### 6.2. Prometheus правила и алерты
- [ ] Создать `dashboard/prometheus-rules.yml`:
  - Recording rules:
    - Агрегированные метрики за 5m, 15m, 30m
    - Prediction error metrics
  - Alerting rules:
    - `MLPredictorDown`: predictor unavailable > 5m
    - `HighPredictionError`: RMSE > threshold
    - `SLOViolation`: latency p99 > SLO
    - `FrequentScaling`: слишком частые изменения реплик

**Файлы**:
```
dashboard/
  prometheus-rules.yml
```

#### 6.3. Логирование
- [ ] Centralized logging (ELK или Loki):
  - Сбор логов всех компонентов (predictor, planner, executor)
  - Structured logs (JSON)
  - Корреляция логов по request_id
- [ ] Dashboard для логов (Kibana или Grafana Loki):
  - Поиск ошибок
  - Трейсинг запросов

#### 6.4. Трейсинг (опционально)
- [ ] Интеграция OpenTelemetry:
  - Трейсинг запросов через predictor → planner → executor
  - Визуализация в Jaeger
  - Анализ bottlenecks

### Критерии завершения Phase 6
- ✅ Grafana дашборд показывает все ключевые метрики
- ✅ Алерты настроены и работают
- ✅ Логирование централизовано и доступно для анализа
- ✅ Визуально видно, что ML-autoscaler работает лучше baseline HPA

---

## Phase 7: Эксперименты и научная публикация

**Цель**: Провести формализованные эксперименты и оформить результаты для публикации.

**Время**: 3-4 недели

**Зависимости**: Phase 6

### Задачи

#### 7.1. Методология экспериментов
- [ ] Документ `docs/experiments.md`:
  - Цель экспериментов
  - Гипотезы:
    - H1: ML-autoscaler снижает latency на X%
    - H2: ML-autoscaler экономит ресурсы на Y%
    - H3: Гибридная модель точнее базовых на Z%
  - Метрики:
    - Качество прогноза: RMSE, MAE, MAPE
    - Производительность: p50, p95, p99 latency
    - Ресурсы: CPU/Memory usage, cost
    - SLO: процент нарушений
  - Экспериментальная установка:
    - Демо-сервис (FastAPI)
    - Нагрузка: синтетические паттерны (daily/weekly seasonality, spikes)
    - Длительность: 7 дней (минимум)
  - Сценарии:
    - Baseline: HPA по CPU
    - Prophet autoscaler
    - LSTM autoscaler
    - Hybrid autoscaler

#### 7.2. Проведение экспериментов
- [ ] Реализовать `tools/experiment_runner.py`:
  - Автоматизация запуска экспериментов
  - Конфигурации сценариев (YAML)
  - Сбор метрик из Prometheus/Grafana
  - Сохранение результатов
- [ ] Запустить эксперименты:
  - Каждый сценарий — минимум 7 дней
  - Или несколько коротких runs с разными паттернами нагрузки
- [ ] Генерация отчётов:
  - Таблицы результатов (CSV, LaTeX)
  - Графики сравнения (matplotlib, seaborn)
  - Статистическая значимость (t-test, Wilcoxon)

**Файлы**:
```
tools/
  experiment_runner.py
  scenarios/
    baseline.yaml
    prophet.yaml
    lstm.yaml
    hybrid.yaml
results/
  experiment_1/
    metrics.csv
    plots/
  experiment_2/
    ...
```

#### 7.3. Обзор литературы
- [ ] Документ `docs/literature-review.md`:
  - Классический HPA и его ограничения
  - Существующие подходы к предиктивному автомасштабированию:
    - Rule-based
    - Time-series forecasting (ARIMA, Prophet, LSTM)
    - Reinforcement Learning (Q-learning, DQN, PPO)
  - Коммерческие решения (Google Cloud Autoscaling, AWS Predictive Scaling)
  - Научные статьи (конференции, журналы):
    - "Predictive Auto-scaling for Kubernetes" (примеры)
    - "Time-Series Forecasting for Cloud Resource Management"
  - Сравнение нашего подхода с существующими

#### 7.4. Черновик статьи
- [ ] Документ `docs/paper-draft.md`:
  - Структура статьи:
    1. **Abstract**: кратко о проблеме, решении, результатах
    2. **Introduction**: мотивация, актуальность, вклад
    3. **Related Work**: обзор литературы
    4. **Problem Statement**: формализация задачи
    5. **Proposed Approach**: архитектура, модели, интеграция с K8s
    6. **Experiments**: методология, setup, результаты
    7. **Results and Discussion**: анализ, таблицы, графики
    8. **Conclusion**: выводы, future work
  - Оформление по требованиям конференции/журнала:
    - РИНЦ (eLIBRARY)
    - Scopus/WoS (IEEE, ACM, Springer)
  - LaTeX шаблон (IEEE, ACM)

**Файлы**:
```
docs/
  literature-review.md
  paper-draft.md
  paper/
    main.tex
    references.bib
    figures/
```

#### 7.5. Презентация
- [ ] Слайды для защиты НИР
  - Актуальность
  - Цель и задачи
  - Архитектура
  - Модели и подходы
  - Эксперименты и результаты
  - Выводы

**Файлы**:
```
docs/
  presentation.pptx
```

### Критерии завершения Phase 7
- ✅ Проведены формализованные эксперименты
- ✅ Результаты задокументированы и статистически обработаны
- ✅ Обзор литературы завершён
- ✅ Черновик статьи написан
- ✅ Презентация подготовлена
- ✅ Подтверждены гипотезы (или опровергнуты с объяснением)

---

## Phase 8: Reinforcement Learning подход (опционально)

**Цель**: Исследовать RL-подход к автомасштабированию как advanced метод.

**Время**: 4-6 недель

**Зависимости**: Phase 7

**Примечание**: Эта фаза опциональна и требует значительных вычислительных ресурсов и экспертизы в RL.

### Задачи

#### 8.1. Формализация как MDP
- [ ] Определить компоненты:
  - **State**: текущая нагрузка, количество реплик, latency, resource usage, time features
  - **Action**: increase replicas, decrease replicas, do nothing
  - **Reward**: 
    - Положительный: соблюдение SLO, экономия ресурсов
    - Отрицательный: нарушение SLO, лишние ресурсы, частые изменения
  - **Transition**: симуляция K8s (или реальный кластер)
- [ ] Документ `docs/rl-formalization.md`

#### 8.2. Симулятор окружения
- [ ] Реализовать `models/rl/env.py`:
  - OpenAI Gym compatible environment
  - Симуляция нагрузки (исторические данные или синтетика)
  - Симуляция Kubernetes (задержка scale-up, pods startup time)
  - Расчёт reward

#### 8.3. RL алгоритмы
- [ ] Реализовать базовый RL агент:
  - Q-Learning / DQN (для дискретных действий)
  - PPO / A3C (для непрерывных или дискретных)
  - Библиотеки: Stable-Baselines3, RLlib
- [ ] Обучение:
  - На симуляторе
  - Hyperparameter tuning
  - Проверка сходимости
- [ ] Тестирование:
  - На hold-out данных
  - Сравнение с ML-autoscaler

#### 8.4. Интеграция с K8s
- [ ] Адаптировать executor для RL агента
- [ ] Online learning (опционально):
  - Дообучение на реальных данных

### Критерии завершения Phase 8
- ✅ RL агент обучен и показывает конкурентные результаты
- ✅ Сравнение RL vs ML-autoscaler задокументировано
- ✅ (Опционально) RL агент задеплоен в реальный кластер

---

## Метрики успеха проекта

### Технические метрики

| Метрика | Baseline HPA | Целевое значение ML-autoscaler |
|---------|--------------|--------------------------------|
| Prediction RMSE | N/A | < X (определить после Phase 2) |
| p99 Latency | Y ms | < Y * 0.9 ms (-10%) |
| SLO violations | Z% | < Z * 0.5% (-50%) |
| CPU usage | 100% | 85-90% (экономия 10-15%) |
| Scaling events | N | < N * 0.7 (-30%, меньше осцилляций) |

### Научные метрики

- [ ] НИР/курсовая/ВКР защищена (оценка 4-5)
- [ ] Статья принята на конференцию или в журнал (РИНЦ/Scopus)
- [ ] Репозиторий получил звёзды на GitHub (цель: 50+)
- [ ] Доклад на студенческой конференции или митапе

---

## Риски и митигация

| Риск | Вероятность | Воздействие | Митигация |
|------|-------------|-------------|-----------|
| Недостаточно исторических данных | Средняя | Высокое | Генерация синтетической нагрузки |
| ML-модели низкой точности | Средняя | Высокое | Гибридный подход, fallback на HPA |
| Сложность интеграции с K8s | Низкая | Среднее | Использование KEDA, хорошая документация |
| Вычислительные ресурсы | Средняя | Среднее | Использование kind/minikube локально |
| Нехватка времени | Высокая | Высокое | Приоритизация задач, опциональные фазы |
| Сложность RL-подхода | Высокая | Низкое | Phase 8 опциональна, можно пропустить |

---

## Контрольные точки 

- [ ] **M0** (конец Phase 0): Локальное окружение работает
- [ ] **M1** (конец Phase 1): Данные собраны и обработаны
- [ ] **M2** (конец Phase 2): Базовые модели обучены
- [ ] **M3** (конец Phase 3): Гибридная модель и ONNX
- [ ] **M4** (конец Phase 4): Predictor сервис работает
- [ ] **M5** (конец Phase 5): Интеграция с K8s завершена
- [ ] **M6** (конец Phase 6): Дашборды готовы
- [ ] **M7** (конец Phase 7): Эксперименты проведены, статья написана

---

## Обратная связь и итерации

Этот roadmap — живой документ. По мере выполнения задач он будет обновляться:

- Добавление новых подзадач
- Уточнение временных оценок
- Корректировка приоритетов
- Документирование принятых решений и trade-offs

Все изменения отслеживаются через Git commits и Issues.
=======
# Roadmap

Файл Roadmap синхронизирован с описанием в README и деталями из запроса. Каждая фаза завершает готовый инкремент и строится на предыдущих.

## Phase 0 — Infrastructure & Repo Skeleton (1–2 недели)

**Цель:** подготовить единый DevEx слой: зависимости, линтеры, Docker-окружение, Kubernetes-манифесты и документацию.

### Выполненные задачи
- [x] `pyproject.toml` + Poetry как единый источник зависимостей.
- [x] Настроены `pre-commit` (black, isort, flake8, mypy, базовые sanity hooks).
- [x] Создан демо FastAPI сервис (`src/k8s_ml_predictive_autoscaling/demo_service`).
- [x] Dockerfile + Compose c 3 экземплярами сервиса, Prometheus и Grafana.
- [x] Grafana provisioning (datasource + стартовый dashboard).
- [x] Prometheus config для скрейпа demo-services.
- [x] K8s manifests (namespace, demo Deployment/Service, Prometheus, baseline HPA, kind config).
- [x] Документация: README, ROADMAP, CONTRIBUTING, `docs/setup-guide.md`.

### Критерии Done
- docker compose запускает демо-сервисы + Prometheus/Grafana локально.
- /metrics собирать Prometheus, графики видны в Grafana.
- Kind конфигурация доступна и применяются базовые манифесты.
- CI (GitHub Actions) гоняет тесты + линтеры.

## Phase 1 — Data Collection & Preprocessing (2–3 недели)

**Зависимости:** Phase 0.

### 1.1 Сбор данных из Prometheus
- [x] `src/k8s_ml_predictive_autoscaling/collector/prometheus_client.py` — высокоуровневый Prometheus API клиент (prometheus_http_api или httpx + PromQL).
- [x] `collect_historical.py` — CLI/скрипт выгрузки временных рядов в `data/raw/YYYYMMDD`. Конфигурация через YAML (`config.yaml`).
- [x] Библиотека конфигурации метрик с поддержкой CPU, памяти, RPS, latency p50/p95/p99.

### 1.2 Генерация синтетической нагрузки (опционально)
- [x] `tools/load_generator/` с k6 и/или Locust скриптами.
- [x] `synthetic_patterns.py` — генерация дневной/недельной сезонности + спайки.
- [x] Интеграция с docker compose (сервис `load-generator`) и K8s Deployment (`k8s/manifests/load-generator-deployment.yaml`).

### 1.3 Exploratory Data Analysis
- [x] `notebooks/research-data.ipynb` — загрузка выгрузок, базовые визуализации и статистики (STL/аномалии добавим позже).
- [x] Документация по итогам EDA (`docs/eda-report.md` с TODO на дальнейший анализ).

### 1.4 Препроцессинг
- [x] `src/k8s_ml_predictive_autoscaling/preprocessor/pipeline.py` — ресемплинг, заполнение пропусков, нормализация.
- [x] `feature_engineering.py` + `anomaly_detection.py` + `config.yaml`.
- [x] Объединённый pipeline с сохранением в `data/processed/{train,val,test}.csv` + scaler.pkl + sliding-window `.npz`.

### Дополнительно (Phase 1 расширения)
- [x] Асинхронный load generator (`k8s_ml_predictive_autoscaling.load_generator`) с Docker/K8s интеграцией.
- [x] Sliding-window датасеты (`sequences_*.npz`) для LSTM/Seq2Seq моделей.

### Критерии Done Phase 1
- Исторические временные ряды сохранены в `data/raw` за N дней/недель.
- Проведён EDA со статами/визуализациями/аномалиями.
- Препроцессинг воспроизводим, с разделением на train/val/test и сохранёнными scaler-объектами.
- Документация по формату данных и признакам добавлена в README/docs.

## Phase 2+ (high level)
- Phase 2 — базовые модели Prophet/LSTM + сравнение (accuracy, latency, затраты).
- Phase 3 — гибрид Prophet+LSTM, экспорт в ONNX.
- Phase 4 — онлайн inference (FastAPI + ONNX Runtime).
- Phase 5 — Resource Planner и интеграция с K8s (HPA/KEDA).
- Phase 6 — Grafana dashboards, метрики эффективности, alerting.
- Phase 7 — нагрузочные эксперименты, отчётность, публикация.
- Phase 8 — исследование RL-подходов (опционально).
>>>>>>> Stashed changes
